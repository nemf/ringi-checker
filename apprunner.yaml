version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Building Docker image..."
run:
  runtime-version: latest
  command: streamlit run ringi_checker.py --server.port=8501 --server.address=0.0.0.0
  network:
    port: 8501
    env: PORT
  env:
    - name: AWS_DEFAULT_REGION
      value: us-east-1
